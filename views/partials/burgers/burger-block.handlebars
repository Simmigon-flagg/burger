<li>
	{{burger_name}}

	 {{!-- <button class="change-devour" data-id="{{id}}" data-newDevoured="{{devoured}}"> --}}
		     <button class="change-devour btn-small waves-effect waves-light" data-id="{{id}}" data-newDevoured="{{devoured}}" type="submit" name="action"> 
		 
		{{#if devoured }} {{else}} Devoured!{{/if}}
	</button> 
</li>
